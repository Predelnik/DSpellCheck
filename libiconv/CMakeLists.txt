cmake_minimum_required (VERSION 3.7)
project (libiconv)
file (GLOB_RECURSE source_files *.c *.h)
add_library (libiconv STATIC ${source_files})
target_compile_definitions (libiconv PRIVATE -DUSING_STATIC_LIBICONV)
target_include_directories (libiconv PUBLIC include/)
